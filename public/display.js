const socket=io();let state=null,audioReady=false,clockOffset=0;const $=id=>document.getElementById(id);$("joinUrl").textContent=location.origin;
const clock=document.createElement("strong");clock.id="clock";clock.style.cssText="color:#fff;font-size:24px;margin-left:12px;font-variant-numeric:tabular-nums";clock.textContent="60s";document.querySelector(".live").append(clock);
async function enableAudio(){if(!audioReady){await import("/audio.js");audioReady=true}SaveJapanAudio.init()}
$("start").onclick=async()=>{await enableAudio();socket.emit("host:start")};$("reset").onclick=()=>{if(audioReady)SaveJapanAudio.stopMusic();socket.emit("host:reset")};
socket.on("state",s=>{state=s;clockOffset=s.serverNow-Date.now();if(s.phase==="playing"&&audioReady)SaveJapanAudio.startMusic();render()});
socket.on("generating",()=>{$("start").textContent="GPT-5.6 IS CREATING MISSIONS…";$("start").disabled=true});
socket.on("pulse",()=>{const u=document.querySelector(".ufo");if(u)u.animate([{transform:"translateX(-50%) scale(1)"},{transform:"translateX(-50%) scale(1.3)",filter:"brightness(3)"},{transform:"translateX(-50%) scale(1)"}],{duration:220})});
socket.on("missionComplete",({victory})=>{if(victory){if(audioReady)SaveJapanAudio.victory()}else{if(audioReady)SaveJapanAudio.alert();document.body.animate([{filter:"brightness(2)"},{filter:"brightness(1)"}],{duration:650})}});
setInterval(()=>{if(state?.phase==="playing"){const left=Math.max(0,Math.ceil((state.endsAt-(Date.now()+clockOffset))/1000));clock.textContent=`${left}s`;clock.style.color=left<=10?"#ff4059":"#fff"}},100);
function render(){if(!state)return;$("playerCount").textContent=state.players;$("regionCount").textContent=state.prefectures.length;$("lobby").classList.toggle("hidden",state.phase!=="lobby");$("battle").classList.toggle("hidden",state.phase!=="playing");$("victory").classList.toggle("hidden",state.phase!=="victory");$("start").classList.toggle("hidden",state.phase!=="lobby");$("start").disabled=false;$("start").textContent="START 60-SECOND DEFENSE";if(state.phase==="lobby"){$("recent").innerHTML=state.recentPlayers.map(p=>`<span class="chip">🚀 <b>${safe(p.nickname)}</b> · ${safe(p.prefecture)}</span>`).join("")}if(state.phase==="playing"){const m=state.missions[state.missionIndex];$("missionNumber").textContent=`MISSION ${state.missionIndex+1} OF ${state.missions.length}`;$("missionIcon").textContent=m.icon;$("missionTitle").textContent=m.title;$("missionBrief").textContent=m.brief;$("score").textContent=state.missionScore;$("target").textContent=`/ ${state.missionTarget} ENERGY`;$("progressBar").style.width=`${Math.min(100,state.missionScore/state.missionTarget*100)}%`;$("regions").innerHTML=state.prefectures.map(r=>`<span class="region">${safe(r.name)} ×${r.count}</span>`).join("");$("ships").innerHTML=state.recentPlayers.slice(0,24).map((p,i)=>`<span class="ship" title="${safe(p.nickname)} · ${safe(p.prefecture)}" style="animation-delay:${i%7*.12}s">🚀</span>`).join("")}if(state.phase==="victory"){$("finalPlayers").textContent=state.players;$("finalRegions").textContent=state.prefectures.length}}
function safe(s){const e=document.createElement("span");e.textContent=s;return e.innerHTML}
